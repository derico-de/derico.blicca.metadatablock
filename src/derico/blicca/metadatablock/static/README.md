# Committed build output

`metadata-block.js`, `metadata-block.js.map` and the scope-wrapped
`metadata-block.css` are written here by `pnpm build` in `../../../../../bundle-src/`
and committed. Nothing is built at install time.
